# Lokal gelistirme bagimliliklari.
#
# Postgres bu dosyada YOK: makinede native olarak 5432'de calisiyor ve
# .env.development zaten ona bakiyor. Sadece eksik olan iki servis burada.
#
# PORTLAR VARSAYILAN DEGIL: bu makinede baska bir projenin compose stack'i
# 6379 (redis) ve 1025/8025 (mailhog) portlarini tutuyor. Ayni Redis'i
# paylasmak BullMQ kuyruk adlarini cakistirir ve o projenin FLUSHALL'i
# bizim kuyrugumuzu silerdi; bu yuzden codxus kendi portlarinda calisiyor.
#
#   docker compose -f infra/docker-compose.yml up -d
#   docker compose -f infra/docker-compose.yml down
#
# Mailpit arayuzu: http://localhost:8026

services:
  redis:
    image: redis:8-alpine
    container_name: codxus-redis
    restart: unless-stopped
    ports:
      - '6380:6379'
    # AOF: kuyruktaki mail isleri container yeniden baslatildiginda kaybolmasin.
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5

  mailpit:
    image: axllent/mailpit:latest
    container_name: codxus-mailpit
    restart: unless-stopped
    ports:
      - '1026:1025' # SMTP
      - '8026:8025' # Web arayuz
    environment:
      MP_MAX_MESSAGES: 500
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

volumes:
  redis-data:
